# 单外层折分层 OOF 试跑

2026-09-05 已完成基础模型阶段的分层训练、预测、校准、拼接和成员隔离审计。
入口：`tools/run_nested_oof_pilot.py`；训练单元：`tools/benchmark_fold_training.py`。
服务器输出：`/root/autodl-tmp/experiments/PARK_repro_01/results/nested_oof_pilot_20260905/`；本地聚合结果：`results/nested_oof_pilot_20260905/`。

## 交付范围

使用开发集 outer fold 0：outer-train 505 人，outer-validation 127 人。四个策略选择折的训练侧分别重建三折下层 OOF。

| 用途 | 基础流水线套数 | 预测人数 |
| --- | ---: | --- |
| 四个策略训练侧的三折下层 OOF | 12 | 各 378/379/379/379 |
| 四个策略验证侧 | 4 | 各 127/126/126/126 |
| 完整 outer-train 四折 OOF | 4 | 505，每人一个预测 |
| 在 outer-train 拟合后预测 outer-validation | 1 | 127 |
| 合计 | 21（84 个网络） | 10 张参与者级预测表 |

每套模型的优化训练、checkpoint 选择、校准、预测成员互斥。各策略训练侧下层 OOF 的拟合祖先及预测对象均不包含对应策略验证成员；outer-validation 不进入任何拟合或选择集合。计划阶段及完成阶段均检查这一边界，并逐套比对实际训练成员清单。

校准拟合与最终 OOF 输出均在参与者平均分数上应用。旧 session 输出只作为私有辅助产物，调度器使用 `private_participant_predictions.csv`。输出不包含疾病标签，尚未接入路由训练。

## 验证与实测

- 21/21 套完成，84 个网络的 epoch 历史长度均与配置一致。
- 总计时 258.616 秒（4.31 分钟）；子进程报告的训练/预测区间合计 124.047 秒。
- 子进程树 RSS 每 0.25 秒采样，最高 901.65 MiB；不含调度器自身，可能漏掉短峰值。
- 最大 PyTorch allocated 131.42 MiB、reserved 176 MiB；不是整个 GPU 进程占用。
- 汇总写入前全部输出 49.28 MiB。
- 源码与产物哈希、实际成员、有限且在 [0,1] 的校准分数、完整无重复的预测覆盖均验证通过。
- 合成测试通过：有效计划被接受，间接使用策略验证成员、重复 OOF 目标、未知祖先和依赖环均被拒绝。
- 未生成锁定队列预测，未计算疾病性能，未训练路由器。

计时从调度器 main 开始，包含分区计划、21 个子进程启动和数据准备、训练、产物审计与拼接；不含调度器自身导入、SSH 和最终汇总写入。仅测一个外层折、一个 seed，不是运行时间置信区间。成员检查依据声明的依赖和实际调用清单，不是对任意 Python 程序的自动数据流证明。

## 基础阶段预算

| 范围 | 流水线套数 | 线性耗时估算 | 输出量级 |
| --- | ---: | ---: | ---: |
| 五个外层折、单 seed | 105 | 21.6 分钟 | 246 MiB |
| 五个外层折、三个 seeds | 315 | 64.7 分钟 | 739 MiB |

估算包含本次基础 OOF 的启动与审计开销，比单套试跑外推更完整；不含压力场景推理、检测器、收益模型、参数选择及 bootstrap，不能当作整个 Fusion Agent 的实验预算。

## 使用和下一步

`execution_plan.json` 记录源码及私有 scope 哈希与人数；`suite_timings.csv` 记录逐套资源；`nested_oof_summary.json` 记录聚合审计。私有成员、模型和预测保存在服务器。

```bash
python tools/run_nested_oof_pilot.py --self-test
python tools/run_nested_oof_pilot.py \
  --fold 0 --seed 101 \
  --output-dir results/nested_oof_pilot_NEW_RUN
```

程序拒绝已有目录，不自动覆盖或恢复不完整运行。该任务沿用已暴露的 v3 开发分区，是工程试跑，不是新冻结协议下的确认性评估；模型结构与恢复特征仍有上游来源局限。

下一步按同样的成员边界接入分区内扰动、检测器、统一无标签路由/风险推理、收益校准与训练侧策略选择。当前缓存只有 clean 疾病分数，不能直接替代原 v3 所需的全部质量特征；也不能复用旧 `tune_margin` 后宣称所有嵌套问题已解决。先完成单外层折路由链验证，再冻结协议、扩展到全部外层折。

# 等覆盖拒绝有效性实验

日期：2026-09-07。状态：主要标准未通过；不授权晋级或外部测试。

## 目的与固定设计

固定可用专家等权均值作为唯一疾病预测分数，不改变融合概率，仅比较六种拒绝风险：

- 简单置信度风险：`1-2*abs(score-0.5)`；
- 可用专家最大概率差；
- 可用专家MC标准差均值；
- 上一实验冻结的置信度层、不确定性层和质量层“均值融合出错”预测器。

使用两种互补评价：

1. `equal_count`：每个外层折、每个场景分别保留风险最低的floor(c*n)人，c固定为90%、80%、70%；所有方法使用相同的、与标签无关的SHA身份顺序处理风险并列，保证实际样本数相同。
2. `fixed_threshold`：每折仅在训练侧正常场景风险分布上取目标覆盖率分位数，随后不变地应用于全部外层场景，检验部署时覆盖率漂移。

主要比较预先固定为80%等覆盖下，不确定性层错误预测器相对简单置信度在14个拟合压力场景中的选择错误率。成功还要求：bootstrap下界>0、至少4/5折改善；正常选择错误率最多恶化0.005；训练侧固定阈值在每个折×场景至少保留20%；正常阳性/阴性覆盖差不超过0.20。

所有风险计算均不读取标签、场景名或折编号；场景和折只用于评价分组。未拟合新模型、未调整预测分数、未使用锁定测试集。等数量模式用于公平比较排序能力，但它假设批量覆盖预算，不是单样本固定阈值部署方案。

## 主要结果

80%目标在逐折floor后合并实际覆盖率为79.43%。14个压力场景的选择结果：

| 拒绝分数 | 选择错误率 | 选择log-loss |
| --- | ---: | ---: |
| 简单置信度 | **0.14357** | **0.38264** |
| 置信度层错误预测器 | 0.15509 | 0.38901 |
| 不确定性层错误预测器 | 0.15581 | 0.38839 |
| 质量层错误预测器 | 0.16050 | 0.39075 |
| 专家分歧 | 0.18042 | 0.40429 |
| MC不确定性 | 0.25470 | 0.49697 |

不确定性层错误预测器相对简单置信度的压力错误率降低量为-0.01224，即实际高1.22个百分点；参与者聚类bootstrap描述性95%区间[-0.02075, -0.00449]，0/5折改善。正常场景也高1.99个百分点。因此主要错误率、区间、折一致性和正常保护均失败。

该方向在次要覆盖率下也没有逆转：

| 目标覆盖率 | 简单置信度错误率 | 不确定性错误预测器错误率 |
| --- | ---: | ---: |
| 70% | 0.11591 | 0.12143 |
| 80% | 0.14357 | 0.15581 |
| 90% | 0.18153 | 0.18934 |

这说明上一实验约0.78的全局错误检测AUROC没有转化为低风险尾部更好的选择错误率。整体AUROC相近不足以证明在具体覆盖预算下更有效。

## 类别覆盖问题

80%等数量正常数据中：

| 方法 | 阳性覆盖率 | 阴性覆盖率 | 选择错误率 |
| --- | ---: | ---: | ---: |
| 简单置信度 | 38.13% | 93.43% | 11.95% |
| 不确定性错误预测器 | 47.50% | 90.25% | 13.94% |

简单置信度的低错误率部分来自大量拒绝阳性参与者；错误预测器稍微改善阳性覆盖，却仍有42.75个百分点的类别覆盖差。因此不能仅凭总体选择错误率把简单置信度宣布为临床可接受方案。

训练侧正常80%固定阈值应用到外层正常数据时，不确定性错误预测器总覆盖80.54%，阳性50.00%、阴性90.89%，类别覆盖差40.89%，失败于预设20%上限。其所有折×场景最小覆盖为59.84%，因此通过最低20%覆盖约束且没有再次出现零覆盖。

相比之下，简单置信度固定阈值在留出的反向笑容冲突中最低仅保留0.79%（某折1人），专家分歧在该场景五折均为零覆盖。错误预测器改善了跨场景覆盖稳定性，但这不能抵消错误选择和类别覆盖标准失败。

## 典型压力场景

80%等数量下，简单置信度与不确定性错误预测器的选择错误率分别为：

| 场景 | 简单置信度 | 错误预测器 |
| --- | ---: | ---: |
| 笑容特征置换 | 0.13147 | 0.14542 |
| 语音高斯0.25 | 0.17729 | 0.17331 |
| 语音高斯0.5 | 0.21116 | 0.20717 |
| 语音高斯1.0 | 0.22112 | 0.20916 |

错误预测器在语音高斯噪声中略好，但在其他场景和正常数据中的损失使压力宏观结果总体更差。这进一步反对用单一场景改善替代预声明总体判定。

## 验证、产物与结论

输入基础缓存和210个上轮模型均通过保存散列核对；632人五折验证成员无重复。交换标签不改变六种风险分数。运行前后输入散列不变，下载后的protocol、thresholds、风险判别、选择指标和decision散列均通过，脚本散列与协议一致。

服务器目录：`/root/autodl-tmp/experiments/PARK_repro_01/results/equal_coverage_rejection_20260907/`；私有逐人选择记录只保留服务器。本地 `results/equal_coverage_rejection_20260907/` 保存聚合协议、阈值、指标和判定。

结论：当前学习式错误预测器不能取代简单置信度进行拒绝；简单置信度虽然总体选择错误率最低，但严重偏向保留阴性，也不能直接晋级。现阶段不应继续微调拒绝阈值或扩大拒绝模型。下一研究决策应回到动作收益路线，单独检验冻结的去语音/去笑容收益预测在执行后是否产生净收益，并加入正常性能和误路由代价约束；若仍失败，应停止现有Agent路线，转向真实质量数据或基础特征表示。

# 完整嵌套 Fusion Agent：本轮完成记录

2026-09-05，本轮从单折工程验证连续完成了协议冻结、五折训练、结果汇总与独立产物审计。任务执行完毕，研究结果未达到内部晋级门槛。

## 实现范围

新增 `tools/nested_agent_pipeline.py`、`tools/run_nested_agent_development.py`、`tools/audit_nested_agent_artifacts.py`、`tools/diagnose_nested_agent_risk.py`。

基础模型采用已验证的 5×4×3 分层方案，每折 21 套模型，五折总计 105 套、420 个网络。三单模态模型与 UFNet 均重新训练；优化训练、checkpoint 选择、概率校准和目标成员隔离。所有下层拟合成员也与对应策略验证成员隔离。

完整 Agent 流程包含：

- 16 个场景；特征高斯噪声、mask、缺失和特征置换对全部相关模型重新推理。分数平移和反向冲突则明确属于分数通道故障。
- 扰动在已切分的预测分区内生成，utility-calibration 与 strategy-selection 的分区先拆开再扰动。
- 质量检测器仅使用合成质量标签；固定浅树参数，不使用外层标签选择模型。
- 各策略验证折按身份哈希拆为收益校准与策略选择两部分，均不进入对应下层拟合；最终收益校准使用 cross-fitted 预测，随后在完整训练侧重拟合。
- 每叶最小行数设为 30 乘以每人最大场景副本数，保证训练叶节点至少有 30 名独立参与者的下界；自动 early stopping 关闭。
- 纯推理函数不读取疾病标签、参与者标识、场景名或折编号。最终 margin 确定后重新计算 score/risk，包括回退情况；缺失模态从冲突和质量风险计算中排除。
- 默认融合为可用校准专家的等权均值，另有去语音、去笑容、笑容收缩和 UFNet 动作。该基线与旧 v3 的 AUROC 加权基线不同，不能把两版差值直接当作架构进步。

## 冻结与验证

开发协议哈希：`6db33a31071abfd9567439c0b7bc01e8f25fa2b7d5082de3fd0aae075f02f538`。

最终工程验证通过后冻结了源码、现有分区标识、场景、选择规则和九项验收门槛。五折期间未修改训练代码或门槛。新增压力场景平均覆盖率下限 50%，并逐场景报告 coverage、accepted_n 和 selective AUC。

独立后验审计确认：105 套基础模型配置一致、84×5 个网络的 epoch 历史完整、632 名外层参与者完整且无重复；实际训练成员与计划一致，跨层边界成立。五个 Agent 在新进程中加载后能复现已保存的 score/action/risk。冻结协议、源码、聚合产物和私有输出的散列验证通过。未生成锁定 Test/global/validation 队列预测。

计时为两路并行五折主流程 861.15 秒，即 14.35 分钟；不包括此前的工程试跑、后验独立审计与下载。输出约 355.81 MiB（最终 summary 写入前）。

## 结果

| 项目 | 结果 | 判定 |
| --- | ---: | --- |
| 内部标准 | 6/9 | 未达标 |
| 启用路由的外层折 | 1/5 | 其余折回退 |
| 正常数据 Agent AUROC | 0.8884 | 基线 0.8905；UFNet 0.8604 |
| 正常 AUROC 差值 | −0.00217 | 通过 −0.005 非劣界限 |
| 压力 log-loss regret | +0.00115 | 失败，损失略高于基线 |
| 笑容特征扰动 AUC 相对最佳固定基线 | −0.00456 | 失败 |
| 收益 bootstrap 下界 | −0.00358 | 失败 |
| 正常覆盖率 | 91.78% | 通过 |
| 正常路由动作率 | 1.26% | 通过 |
| 正常 ECE 差值 | +0.00365 | 通过 |
| 正常非劣的折数 | 4/5 | 通过 |
| 主要压力场景平均覆盖率 | 52.27% | 通过平均门槛 |

六个语音扰动场景的 coverage 都为零；反向笑容分数冲突 coverage 约 0.16%。因此通过总体平均覆盖率门槛不代表这些场景能够提供有效决策。拒绝不是已经证明的鲁棒性收益。

### 零覆盖的后验诊断

保持冻结策略不变，重新加载五个 Agent，对已有外层预测逐项重建风险，数值一致性检查通过。六个语音场景 × 五折的 30 个组合全部拒绝。语音风险项均值约 0.34852（对应质量检测分数约 0.99576），接近该项上限 0.35；但各折拒绝阈值为 0.35877–0.41520，语音项单独超过阈值的比例为零。

叠加笑容质量、专家分歧和预测不确定性后，各组合最小总风险为 0.45930–0.52130，均超过对应阈值。笑容风险项的组合均值为 0.11791–0.13310。由此确认直接机制是多项风险相加导致整体拒绝，而不是语音项单独越界。不同语音扰动强度下语音风险项均值几乎相同，也提示当前质量分数不能直接视为任务错误概率；这尚不能证明某一风险项应删除。

诊断文件为 `risk_component_audit.csv` 和 `risk_diagnostic_summary.json`；CSV SHA-256 为 `6e571f9b5c05d4a7a8d0947fcb3bb5862971452e7c1e9d4cb8bef05296317bd8`。这是后验解释，不是新的验证集调参或独立泛化证据，未修改阈值、模型或原始验收判定。

本轮结果表明当前预设路由策略没有达到要求，不证明所有 expected-regret 方法均无效。此前 v3 的约 0.95 正常 AUC 与本轮不能直接比较：基础训练成员、校准、基线及扰动语义均有变化。既有开发数据暴露、上游结构选择和恢复特征来源仍限制最终泛化结论。Bootstrap 是开发量，不是搜索后无偏确认性结论。

## 交付和停止点

服务器目录：`/root/autodl-tmp/experiments/PARK_repro_01/results/nested_agent_v4_development_20260905/`。
本地聚合报告：`results/nested_agent_v4_development_20260905/NESTED_AGENT_REPORT.md`；同时保存 protocol、acceptance checks、完整逐折指标、计时及 postrun audit。

本轮授权的完整流水线任务已经完成；未启动大模型训练或 GitHub 推送。后续研究重点是质量风险与任务错误是否一致、语音场景零覆盖，以及路由是否有足够可学习增益；需要另行预声明研究设计，不应根据此次外层分数直接放宽门槛追求通过。当前没有需要用户反复确认的未完成工程步骤。

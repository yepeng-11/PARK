# V3.1 实现审计与 OOF 训练计划

审计日期：2026-09-05。基线提交：`2d32688`。本次为静态代码审查、已有聚合结果复核及服务器只读资源检查；没有修复训练代码或启动新实验。

## 结论与证据边界

v3.1 的已有数值仍是历史程序运行输出，但不能称为“严格执行冻结协议的嵌套评估”。它存在基础模型训练成员重叠、元学习层间标签依赖、校准缺失等问题。5/8 标准的结果不足以判定 expected-regret 方法本身失败，也不支持晋级。后续需先修正实现和实验协议，再重跑开发评估；换折不能消除既有开发集暴露。

## 问题清单

| ID / 优先级 | 代码证据 | 影响 | 修复与验收 |
| --- | --- | --- | --- |
| A01 / P0 | `train_fusion_agent_v3_router.py:540` 及 `train_fusion_agent_v2_specialists.py:367`：基础模型从 `paired_retraining/cleaned/seed_*` 加载；`train_paired_baselines.py:525` 使用原始 Train/Dev | 基础模型及 scaler 未按 v3 折重新训练；原始 Dev 还参与 checkpoint 选择，因此从优化训练集排除也不等于未见 | 为每个预测记录优化训练、checkpoint 选择、校准和超参数选择成员；被预测参与者与全部依赖成员集合无交集 |
| A02 / P0 | `build_outer_oof` 为各 inner fold 建特征，`tune_margin` 再从这张 OOF 表留出一折 | 例如验证折 A 的标签可通过为 B 折拟合的基线权重进入 B 的特征；用 B 训练元模型、再验证 A 不是完整层间隔离 | 在每个元验证折的训练侧重新生成其下层 OOF 数据；不能只切已有 OOF 表 |
| A03 / P1 | `train_fusion_agent_v3_router.py:540` 复用同编号 v2 specialist 参数；v2/v3 split seed 不同 | v2 超参数选择范围可能包含当前 v3 外层参与者；synthetic target 虽非疾病标签，仍构成检测器选择的数据暴露。实际逐折人数本次未读取私有清单计算 | 重新在本折训练侧选择参数，或预先固定且明确登记为既往开发选择；不得再称无暴露 |
| A04 / P1 | 协议 `router_contract.calibration` 对比 `action_and_feature_frame`、`fit_loss_models`：未拟合疾病分数和收益校准器 | 计算 ECE 不等于执行校准；违反冻结契约 | 独立训练侧校准分区或完整嵌套校准；保存校准器及成员证明；校准器不能看到它所生成 OOF 预测的标签 |
| A05 / P1 | `predict_scenarios` 在整个 development 上 `rng.shuffle(permuted)`，随后 `make_records` 才按分区筛选 | 分区间分数流动；不属于直接标签置换，但违反分区内扰动约束 | 分区切分后生成扰动；修改分区外输入不应改变该分区扰动输出 |
| A06 / P1 | `tune_margin` 更新 chosen_action/score 后未更新 risk；失败分支返回原始启用路由的 cross_fitted；主程序用其 risk 拟合阈值 | clean coverage 与最终 margin/fallback 策略不匹配 | 抽取统一无标签推理函数；选定策略后重算全部 OOF score/risk，再确定拒绝阈值；覆盖率需重新评估 |
| A07 / P1 | `fit_loss_models` 的 `min_samples_leaf=30`；每人有多场景训练记录 | 30 行不能保证协议要求的 30 名独立参与者；场景重复还影响训练权重 | 对叶节点独立参与者数执行硬审计，或修订可实现的模型约束；显式冻结参与者/场景权重 |
| A08 / P1 | margin 的 stress 集包含 `smile_opposite_consensus`，虽然该场景 `trainable=False` | 不参与 loss 模型拟合，但参与阈值选择，不能称纯留出 stress 测试；同一 OOF 表还用于 margin 搜索和 bootstrap 下界筛选 | 为模型拟合、策略选择、最终开发评估定义不同用途；bootstrap 不能作为搜索后仍独立的 95% 确认证据；对纯 stress 场景禁用所有选择用途 |
| A09 / P1 | smile 场景仅改变 clean.smile，UFNet、MC std 和部分描述符保持原值 | 代表 score 通道故障；UFNet 是未损坏的另一通道，因此不能解释为对同一原始模态噪声的鲁棒性比较 | 分别登记 score 故障和原始特征故障；后者必须让全部依赖该模态的模型重新推理；描述符随故障语义一致更新 |
| A10 / P1 | `apply_loss_router` 的 spread 使用全部专家，即使 availability=0；收益判定是策略总体 bootstrap + 单例点估计 | 缺失模态仍可影响拒绝；协议“每个非默认动作置信下界为正”与实际总体策略下界含义不明确 | 对 unavailable 专家排除 spread/不合理差值；明确采用策略级还是样本级证据，冻结同一实现语义 |
| A11 / P1 | `apply_loss_router` 读取 label 并计算 actual_regret_improvement | 分数路径未直接使用 label，但部署推理 API 不能处理无标签输入，混合了推理与事后评估 | 分离 predict 与 evaluate；改标签或去掉标签时 chosen_action/score/risk 必须保持一致 |
| A12 / P2 | `load_protocol` 只检查 manifest 文件哈希；`--force` 声明但未控制覆盖；run manifest 未记录训练脚本哈希 | 无法充分证明所有契约文件和代码版本；重跑可覆盖既有输出 | 校验全部契约哈希；默认拒绝非空结果目录，记录源码/配置/依赖哈希；每次运行独立目录 |

A01、A02、A04 和 A06 是当前优先阻断项。已有 `protected_artifacts_unchanged=True` 只说明列出的文件未变化，不证明模型训练成员隔离。23/23 split checks 同样只验证分区结构，不覆盖拟合依赖。

风险拒绝还需报告每个场景的样本数、覆盖率和选择后指标；此前多个语音扰动场景覆盖率为零，不能把全样本 AUROC 当作实际保留决策的表现。

## 建议的新训练边界（待实现，不是已经完成的协议）

保留 5 个外层开发折，每个外层训练侧做 4 个策略选择折；每个选择折的训练侧再做 3 折下层 OOF。每套基础模型内，优化训练、checkpoint 选择、校准的参与者继续隔离，并全部属于该套模型允许的训练侧。预测目标不进入任何一种拟合或选择过程。

在每个 outer-train 中：

1. 每个策略选择折 H：仅用其训练侧 T，生成 3 折下层 OOF，训练收益模型；另拟合一套只依赖 T 的基础预测流水线，为 H 生成验证输入。检测器、权重和校准也服从相同边界。
2. 汇总四个 H 的验证决策，选择预声明策略参数；这属于开发选择，不是最终确认。
3. 为完整 outer-train 生成 4 折基础 OOF，拟合最终收益模型；再用 outer-train 拟合一套基础流水线，预测 outer-validation。
4. outer-validation 只用于记录开发性能。拒绝阈值由最终策略对应的训练侧 OOF 风险确定；所有额外校准必须有明确训练侧依赖。

第一版保持模型结构和超参数固定；把架构搜索、增加 seeds 和路由网格扩展留到流水线验证之后。基础训练可复用 `train_predictor`、`train_fusion`、`fit_training_scalers`，不能直接调用 `train_one`：后者会自动评估 released Test/global/validation 分区。

## 资源估算

2026-09-05 服务器只读观测：RTX 2080 Ti，显存 11264 MiB（观测时已用 0）；`/root/autodl-tmp` 可用约 49 GB；现有 cleaned/seed_101 全目录约 2.5 MB。目录大小包含旧预测和训练记录，仅用于量级比较，不能代表新任务峰值空间。

| 范围 | 基础流水线套数（每套三单模态模型加一个 UFNet） | 神经网络训练次数 |
| --- | ---: | ---: |
| 只做 5×4 基础 OOF 加外层最终模型 | 25 | 100；不足以支持这里要求的全层嵌套路由选择 |
| 上述 5×4×3 方案，单 seed | `5 × [4 × (3+1) + 4 + 1] = 105` | 420 |
| 上述方案，三个独立 seeds | 315 | 1260 |

这是未复用缓存、未增加超参数搜索的显式训练计数。每套划分独立的 checkpoint/校准集不会自动增加网络数；若选择再训练或交叉校准集成，则须追加次数。每套还需 scaler、检测器和元模型训练。

历史 `results/paired_retraining/run_manifest.json` 的 `elapsed_seconds=1.3826` 不能作为耗时依据：`train_paired_baselines.py` 在计时开始后跳过已完成 runs，最后仍重写 manifest。这与跳过运行相符，但本次未通过历史日志确认该次调用经过。

令单套完整训练实测时间为 t 分钟、折训练及推理总额外时间为 u，保守预算为 `105t + u`；三 seeds 为 `315t + u_3`。示例仅供排期：t=5/15/30 分钟时，单 seed 训练部分为 8.75/26.25/52.5 小时，均非实测预测。不能据当前信息承诺几小时完成。

按旧目录量级，105/315 套约 263/788 MB，但 OOF 特征缓存、MC 样本和日志会增加空间。默认只保存 MC 均值/方差；缓存大小按 `预测行数 × 特征列数 × 数值字节数` 加文件开销计算。单 GPU 顺序执行，峰值显存不会按 105 倍增长；实际峰值仍须计时试跑测量。

## 修复顺序与验收

1. 完成成员依赖 DAG/清单、分区内扰动、无标签 predict 接口和覆盖保护；先做合成参与者测试。
2. 实现折内基础训练、独立 checkpoint 选择及校准；先跑单套完整配置，记录分阶段 wall time、峰值 GPU/RAM、磁盘增量和训练样本数。
3. 完成一个 outer fold 的端到端试运行，验证模型/特征的全部祖先成员与对应验证集无交集；完成后估算全量资源。
4. 冻结修订协议与代码哈希，再运行 105 套单 seed 开发实验。只有隔离、数值、资源审计通过后才扩大 seeds。

必要测试：扰动输入跨分区独立性；改动验证标签不改变训练产物；无标签/打乱标签不改变推理输出；最终 margin 与 fallback 的风险重算；availability 对 spread 的影响；叶节点独立参与者计数；拒绝契约哈希变化与已有输出覆盖。不得把这些测试简化为仅检查文件行数或终止标志。

这次工作到审计与资源方案为止。下一次实施入口是第 1、2 项；大模型训练和新路由架构搜索不在当前建议范围内。

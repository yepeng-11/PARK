# 实验二：全覆盖简单融合与质量特征消融

2026-09-05。已完成 5 折、20 个固定小模型；未重训基础网络、未启用拒绝、未使用锁定测试集。结论：简单逻辑回归是后续诊断候选，但没有稳健替代基线的证据；当前质量描述符未证明增量价值。

## 协议和实现

脚本 `tools/run_fusion_experiment2.py` 在首次拟合前写入协议、源码和输入散列。复用此前通过独立成员审计的 `private_final_train.csv` 与 `private_outer_validation.csv`，校验缓存与原始 agent_summary 的 SHA-256 一致，训练和外层参与者无交集，632 人在外层仅出现一次，每人 16 场景且标签一致。固定参数、不做内层搜索或基于外层选择阈值，因此本轮只拟合最终训练侧基础 OOF 数据，不把现有外层预测重新切分用于训练。

所有模型使用 15 个允许拟合的场景；每人总训练权重为 1，正常占 0.5，其余 14 个扰动均分 0.5。反向笑容冲突只评估、不拟合。沿用基础概率校准，不额外校准新融合输出。

- 基线：可用专家等权均值、UFNet。
- 逻辑回归：三个专家及 UFNet 的 logit、可用性，训练侧标准化，C=1，最多 2000 次迭代，未启用类别重加权。
- 浅树基础版：相同输入，HGB 分类器直接预测疾病概率。
- 浅树不确定性版：加三专家 MC 标准差、可用专家分歧及概率距 0.5 的距离。
- 浅树质量版：进一步加入三模态原有特征描述符，不使用合成损坏标签或新检测器。

三个浅树采用相同参数：深度 2、最多 4 叶、100 轮、学习率 0.05、L2=2、关闭 early stopping，叶节点至少 450 行（每人最多 15 行，因此独立参与者下界 30）。它们是低复杂度非线性融合，不是显式动作选择器；本实验没有直接训练实验一的理想动作标签。

主要报告：14 个压力场景先按人平均的 log-loss 收益，以及质量版相对不确定性版的增量。正常五折平均 AUROC 保护界限为 -0.005；逐场景结果必须同时查看。bootstrap 按参与者、折及类别分层，整组保留同一人的所有场景，1000 次；区间仅用于已暴露开发数据的描述，不是无偏确认性或多重比较校正后的结论。

## 正常数据结果

以下均为五折指标等权平均，所有模型 coverage=100%。

| 方法 | AUROC | log-loss |
| --- | ---: | ---: |
| 等权均值 | 0.890535 | 0.423866 |
| UFNet | 0.860433 | 0.398815 |
| 逻辑回归 | 0.889725 | 0.365075 |
| 浅树基础版 | 0.876512 | 0.377717 |
| 浅树不确定性版 | 0.874899 | 0.379056 |
| 浅树质量版 | 0.868649 | 0.386502 |

逻辑回归正常 AUROC 差 -0.000810，通过预声明的均值保护界限；三个浅树分别下降 0.014022、0.015636、0.021886，均未通过。逻辑回归正常 log-loss 在 5/5 折改善，按人合并收益 0.058778，描述性区间 [0.036138, 0.080531]。

合并五折预测的正常 AUROC：均值 0.890572、逻辑回归 0.880932，差 -0.009640。它与折内排序平均不同，可能涉及跨折分数尺度差异，但本轮未证明原因。不能用平均折指标掩盖合并口径下降，也不能把事后另一口径改写为本轮预声明门槛。

## 压力场景收益及失败模式

正收益表示相比等权均值损失下降。

| 方法 | 压力 log-loss 收益 | 描述性 95% 区间 | 收益为正的折 |
| --- | ---: | --- | ---: |
| 逻辑回归 | 0.016151 | [-0.002059, 0.033377] | 5/5 |
| 浅树基础版 | 0.019371 | [0.003152, 0.035302] | 5/5 |
| 浅树不确定性版 | 0.020915 | [0.004415, 0.036560] | 5/5 |
| 浅树质量版 | 0.018865 | [0.000897, 0.035246] | 4/5 |

逻辑回归平均压力收益区间跨零；浅树虽平均压力损失较低，但牺牲正常 AUROC。没有一个候选同时提供正常保护和清晰的总体压力收益证据。

关键逐场景 log-loss（合并参与者）：

| 场景 | 均值 | 逻辑回归 | 解读 |
| --- | ---: | ---: | --- |
| 语音高斯 0.25 | 0.474529 | 0.498243 | 恶化 |
| 语音高斯 0.5 | 0.522037 | 0.617088 | 明显恶化 |
| 语音高斯 1.0 | 0.527137 | 0.656163 | 明显恶化 |
| 语音遮蔽 0.1 | 0.421461 | 0.360289 | 改善 |
| 笑容特征置换 | 0.453385 | 0.412494 | 改善 |
| 留出反向笑容冲突 | 0.631954 | 0.717301 | 恶化；不进入主要压力均值 |

在留出反向冲突中逻辑回归 AUROC 上升而 log-loss 恶化，再次说明二者不能相互替代。不能把平均压力收益解释为每类故障均稳健。

## 质量特征是否有增量？

质量版相对不确定性版：正常 log-loss 收益 -0.007460（5 折全部为负），正常平均 AUROC 再下降 0.006250；压力收益 -0.002050，描述性区间 [-0.008415, 0.003878]。不确定性版相对基础版压力收益 0.001544，区间 [-0.001592, 0.004466]。

因此这套固定模型和权重下，未观察到质量描述符的可靠增量。不能推断所有质量信息无用，也不能把浅树未奏效解释成数据不存在可学习信号；该结论仅覆盖本次模型容量、输入和训练方案。

## 验证、产物与下一步

20 个模型均通过无标签推理与 pickle 序列化往返一致性检查；输入 SHA-256 运行前后不变。下载后源码和聚合指标散列一致。计时约 4.27 秒，为缓存核验及协议冻结之后的小模型拟合/评估阶段，不含既往 420 个基础网络训练、上传和下载。本轮未另做全套基础成员审计，继承先前审计并核验缓存；没有声称重新执行过独立全流程审计。

服务器：`/root/autodl-tmp/experiments/PARK_repro_01/results/fusion_experiment2_20260905/`，私有模型和预测留在服务器。本地 `results/fusion_experiment2_20260905/` 包含 protocol、summary、metrics 和 comparisons。

建议停止扩展当前质量路由。下一项应是有限的“收益来源与强语音噪声失败诊断”：区分简单校准的贡献与专家组合的贡献，检查跨折分数尺度，以及语音噪声下概率损失恶化的原因。任何新拟合应另行冻结；不能根据本次外层结果直接调参后宣称验证通过。实验三如开展，应固定预测器、单独比较拒绝分数，不能把拒绝用于遮盖本次全覆盖失败。未启动实验三或大模型训练。

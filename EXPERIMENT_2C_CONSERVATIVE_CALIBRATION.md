# 实验二补充 C：固定保守校准

2026-09-05。已完成，未训练模型、未修改历史结果、未启用拒绝、未评估锁定测试集。

## 问题和固定设计

检验全局减弱校准是否能同时保留正常 log-loss 收益并避免语音高斯噪声损失。使用前轮校准器已生成的同一批 632 人、五折、16 场景外层分数。

公式：`sigmoid((1-alpha)*logit(raw_mean) + alpha*logit(calibrated_mean))`。预先固定 alpha=0、0.25、0.50、0.75、1；0 和 1 分别为原始均值和完整校准。没有基于外层标签拟合或选择 alpha，没有进一步搜索更小间隔。

拟合为零不意味着独立验证：这是由既有外层发现驱动的后验开发消融。采用正常平均损失改善、语音高斯平均损失不恶化的点估计共同检查，仅用于描述，不构成晋级许可。按参与者、折和类别分层的 1000 次配对 bootstrap 保持场景整体，区间未校正多重比较。

## 结果

正常及最强语音高斯场景的 log-loss（632 人合并，越低越好）：

| 校准强度 | 正常 log-loss | 语音高斯 1.0 log-loss | 语音高斯 1.0 高置信度错误人数 |
| --- | ---: | ---: | ---: |
| 原始均值 0% | 0.423923 | 0.527137 | 2 |
| 25% | 0.399915 | 0.533501 | 4 |
| 50% | 0.382077 | 0.546678 | 8 |
| 75% | 0.369414 | 0.565739 | 10 |
| 完整校准 100% | 0.361077 | 0.589831 | 13 |

高置信度错误指固定 0.5 阈值下判错且所预测类别概率至少 0.9。所有场景 coverage=100%。所有变体每折每场景 AUROC 与原均值一致，因为变换为正斜率单调函数；这些损失变化不是判别排序能力提升。

相对原均值的 log-loss 收益（正为改善）：

| 校准强度 | 正常收益 | 14 压力场景平均收益 | 3 个语音高斯场景平均收益 [描述性 95% 区间] |
| --- | ---: | ---: | --- |
| 25% | 0.024008 | 0.015619 | -0.001429 [-0.005680, 0.002461] |
| 50% | 0.041846 | 0.024804 | -0.009795 [-0.018295, -0.002186] |
| 75% | 0.054509 | 0.028597 | -0.024114 [-0.036929, -0.012739] |
| 100% | 0.062846 | 0.027891 | -0.043496 [-0.061007, -0.028095] |

所有非零强度正常及主要压力平均收益在五折为正，但没有一个非零强度同时满足正常收益为正、语音高斯收益点估计非负。总体压力平均改善掩盖了特定故障的恶化。

25% 校准在轻语音高斯 0.25 下改善 0.007664，在中等 0.5 下恶化 0.005587，在强 1.0 下恶化 0.006364。强噪声错误率从原均值 25.79% 上升到 29.11%，而完整校准为 33.39%。25% 确实缓解副作用，但不能称为没有代价。

## 解释与决定

1. 全局校准强度存在正常收益与特定噪声损失的取舍。本次三个固定中间强度没有消除取舍；不能推断所有实数 alpha 都不可能奏效。
2. 25% 高斯收益区间跨零不等于证明非劣；本轮没有预声明非劣裕量，也没有足够独立验证。不要因为它较温和就直接部署或选择为最优模型。
3. 不继续细搜 10%、15%、20% 等强度以追逐已暴露外层结果。保留原均值、完整校准以及本次完整曲线作为对照，不选定获胜模型。
4. 当前结论是静态校准不能解决已观察到的可靠性问题。若继续原定研究路线，下一项应回到独立的拒绝有效性实验：固定预测器，在训练侧确定阈值，并比较相同覆盖率下的任务错误；同时报告部署阈值在所有场景的实际覆盖率。该实验检验能否识别错误，不再混合修改融合预测。

## 验证和产物

脚本 `tools/evaluate_conservative_calibration.py`。输入预测与前轮私有散列一致、五折成员无重复、标签与场景完整；两端分数复现、每折 AUROC 不变均通过；运行前后输入散列不变，本地下载后脚本和聚合指标散列核验通过。

服务器：`/root/autodl-tmp/experiments/PARK_repro_01/results/conservative_calibration_20260905/`。本地 `results/conservative_calibration_20260905/` 保存 protocol、summary、metrics、comparisons。未上传 GitHub，未启动拒绝实验或大模型训练。

# 质量信号—任务错误—动作收益可预测性实验

日期：2026-09-07。状态：开发实验完成；质量描述符增量标准未通过，不授权晋级或外部测试。

## 研究问题

在不使用参与者标识、真实标签、场景名、折编号或合成损坏标签作为推理输入的前提下，检验部署时可见信号能否预测：

1. 手指、语音、笑容、UFNet及可用专家均值是否分类错误；
2. 上述输出的实际 log-loss；
3. 去语音或去笑容是否比可用专家均值降低 log-loss，以及降低多少。

本实验回答“信号是否可预测错误/收益”，不直接证明按预测执行动作后能改善最终系统。

## 隔离设计

每个外层折只使用 `private_final_train.csv` 的15个允许拟合场景训练，在对应 `private_outer_validation.csv` 评估。训练/评估参与者无重叠，五个外层验证集合并为632人（160阳性），每人只属于一个外层折。基础缓存继承完整嵌套实验的独立审计，并再次校验原始 `agent_summary` 中的SHA-256、标签一致性、场景完整性及每人场景数。

每人训练总权重为1：正常场景0.5，其余14个拟合场景均分0.5。反向笑容分数冲突不参与拟合。动作收益任务在目标模态缺失或动作分数与均值完全相同时排除该行。

三层固定输入消融：

- `confidence`：三个专家和UFNet的校准概率/logit、距0.5的置信度、三个可用性标记；
- `uncertainty`：增加三个MC标准差、可用专家spread和三个成对分歧；
- `quality`：进一步增加三模态已有特征描述符。

每个层级和任务各拟合一个错误/收益分类器及连续损失/收益回归器。全部采用固定浅层HistGradientBoosting：100轮、深度2、最多4叶、学习率0.05、L2=2、叶节点至少450行、关闭early stopping。每人最多15行，因此叶节点独立参与者数下界为30。五折共210个固定预测器，没有超参数搜索。

主要指标在14个拟合压力场景上，先在每折计算三个任务的AUROC宏平均，再对五折平均：均值融合错误、去语音获益、去笑容获益。质量增量预设条件为：相对`uncertainty`至少+0.01、参与者聚类bootstrap下界>0，并且至少4/5折方向为正。绝对可预测性只设描述性标记：质量层宏平均AUROC至少0.65。

## 主要结果

| 输入层 | 三任务五折宏平均AUROC |
| --- | ---: |
| 置信度 | 0.780256 |
| +不确定性与分歧 | 0.788490 |
| +质量描述符 | 0.792058 |

质量层相对不确定性层增量为+0.003568，参与者聚类bootstrap描述性95%区间为[-0.000198, 0.007768]，4/5折为正。它通过绝对可预测性标记，但没有达到+0.01增量门槛，且区间跨零，因此`quality_increment_supported=false`。

五折质量层宏平均分别为0.7629、0.8006、0.8009、0.8025、0.7935；不由单一折驱动。区间重采样在每折疾病类别内抽取参与者，并保留其全部场景和任务；不重新拟合模型，未进行多重比较校正。

## 三个主要任务的拆解

以下为合并外层预测的压力场景描述性指标；合并AUROC与主要判定的“折内AUROC后平均”口径不同。

| 任务 | 事件率 | 置信度 AUROC | 不确定性 AUROC | 质量 AUROC | 质量 AUPRC |
| --- | ---: | ---: | ---: | ---: | ---: |
| 均值融合出错 | 0.2184 | 0.7725 | **0.7778** | 0.7699 | 0.4308 |
| 去语音降低损失 | 0.3306 | 0.7851 | 0.7910 | **0.8063** | 0.6972 |
| 去笑容降低损失 | 0.5141 | 0.7932 | 0.8067 | **0.8104** | 0.7977 |

质量描述符对动作收益预测有方向性帮助，尤其去语音任务；但对均值融合错误预测没有增量，反而低于不确定性层。宏平均的小幅提升不能解释成统一的“质量信号有效”。

连续目标也呈现相同结构：质量层预测实际均值log-loss的Spearman相关为0.7453，低于不确定性层0.7687；预测去语音收益为0.3424，高于不确定性层0.2824；预测去笑容收益为0.2747，高于0.2407。质量层三个任务MAE都优于对应目标的常数中位数基线，但这不等于按其决策一定带来净收益。

## 场景迁移限制

- 质量层对语音高斯场景中的“去语音获益”预测AUROC为0.8112，但对均值融合错误的AUROC只有0.7175，明显低于其在笑容特征扰动中的0.7989。
- 训练时完全留出的反向笑容分数冲突中，“去笑容获益”的事件率为81.96%，质量层AUROC仅0.5832。AUPRC为0.8634主要受高事件率影响，不能替代AUROC判断排序能力。
- 这说明已有特征质量描述符对某些特征级扰动有信息，却没有证明能迁移到不同机制的分数通道冲突，更不能外推至真实采集故障。

## 审计与执行记录

210个模型均完成无标签特征不变性检查；每折保存后重新加载模型，预测逐元素一致。输入文件在运行前后散列不变。聚合包下载后，protocol、metrics、primary fold macro和decision散列均通过，分析脚本散列与冻结协议一致。

首次运行的模型拟合成功，但旧版DataFrame聚类bootstrap效率不足，在产生最终decision/summary前被主动终止；未使用该未完成目录。随后使用数学等价的参与者×场景矩阵重采样，在新目录从头完整运行。最终有效目录带`_v2`，不把半成品混入结果。

服务器最终目录：`/root/autodl-tmp/experiments/PARK_repro_01/results/quality_task_predictability_20260907_v2/`。私有预测和模型仅保留服务器。本地 `results/quality_task_predictability_20260907_v2/` 保存协议、汇总、完整聚合指标、五折主要宏平均和判定；没有锁定测试预测。

## 结论与下一研究决策

本实验支持以下有限结论：部署可见的置信度、不确定性和分歧能够预测一部分任务错误及动作收益；因此“收益完全不可预测”不成立。它不支持“现有质量描述符提供可靠统一增量”，也不证明执行动作后系统性能会提高。

后续不应继续扩充质量描述符或训练更复杂统一路由。更有证据的两条窄路线是：

1. 固定融合预测器，用`uncertainty`层的均值错误预测器与简单置信度进行等覆盖拒绝比较；
2. 单独评估去语音/去笑容收益预测器在预声明阈值下产生的真实净收益、误路由代价与正常性能保护，尤其检验未见冲突迁移。

按此前计划，优先进行第一项拒绝有效性实验，因为它不同时修改融合预测；拒绝必须逐场景报告覆盖率和选择风险，不能再次以零覆盖形成表面性能。所有现有数据已暴露，任何后续结果仍属于开发证据，最终主张需要新参与者或冻结后的一次性外部验证。

# 实验一：专家互补性与动作收益上限

2026-09-05。状态：已完成后验诊断；未训练模型、未更改阈值、未评估锁定测试集。

## 数据与验证

使用已通过独立审计的 nested_agent_v4_development_20260905 五折外层预测：632 名参与者（160 阳性），每人 16 个场景，每场景每人一行。重建全部五个动作分数并与历史 decisions 逐项比较（1e-12 容差）；确认五折参与者无重叠、标签一致、场景完整。运行前后输入文件 SHA-256 不变；下载后聚合文件及分析脚本散列验证通过。

脚本：`tools/analyze_expert_complementarity.py`。服务器聚合目录：`/root/autodl-tmp/experiments/PARK_repro_01/results/expert_complementarity_20260905/`；本地同名 `results/` 子目录。生成 96 行场景/折汇总、1152 行有方向的专家补救统计、480 行动作指标及 manifest。

## 1. 专家确实存在错误互补

在正常数据、固定 0.5 分类阈值下，三单模态专家全部错误比例为 8.70%，有对有错比例为 26.90%。这不意味着现有融合可以纠正全部分歧。

| 出错来源 | 来源错误人数 | 补救专家 | 补救人数 | 条件补救率 |
| --- | ---: | --- | ---: | ---: |
| 手指 | 164 | 语音 | 95 | 57.93% |
| 笑容 | 157 | 语音 | 92 | 58.60% |
| 语音 | 104 | 手指 | 35 | 33.65% |
| 语音 | 104 | 笑容 | 39 | 37.50% |

条件补救率指来源专家判错的人中，另一专家判对的比例；不同补救专家可能覆盖相同人，不能相加。缺失模态不作为错误专家进入补救统计。

## 2. 当前动作集合有事后收益空间

动作固定为：available_mean、drop_speech、drop_smile、shrink_smile、ufnet。对每人使用真实标签选择 log-loss 最小动作，得到不可部署的理想选择器。最佳固定动作也按本场景结果事后选取。

| 场景 | 均值 log-loss | 事后最佳固定动作 | 最佳固定 log-loss | 理想 log-loss | 理想相对最佳固定收益 |
| --- | ---: | --- | ---: | ---: | ---: |
| 正常 | 0.42392 | UFNet | 0.39883 | 0.27233 | 0.12650 |
| 语音高斯 1.0 | 0.52714 | 去语音 | 0.47961 | 0.30873 | 0.17088 |
| 语音遮蔽 0.1 | 0.42146 | UFNet | 0.39184 | 0.26978 | 0.12206 |
| 笑容特征置换 | 0.45338 | 去笑容 | 0.41410 | 0.29462 | 0.11948 |

正常场景五折各自的理想收益为 0.08583–0.14271；并非仅由某一折贡献。按折及类别分层的参与者 bootstrap（1000 次，每次重新选事后最佳固定动作），正常合并理想收益的描述性中央 95% 区间为 [0.10543, 0.14593]。

重要限制：逐样本取最小损失使理想收益按构造非负，区间为正不是可学习性或显著可部署收益的证明。它也不是 AUROC 上限；甚至纯随机的动作差异也可能产生事后选择收益。仍需独立的、无标签输入的收益预测实验检验。没有设立事后通过门槛，以上结果仅支持继续做有限的可预测性诊断。

## 3. 不同故障不能使用同一种降权规则

强语音高斯噪声（1.0）下，去语音相对均值平均 log-loss 改善 0.04752，五折事后最佳固定动作均为去语音。

轻度语音遮蔽（0.1）下，去语音反而使 log-loss 增加 0.05741；正常数据去语音也增加 0.05756。结合上一轮语音场景全拒绝的事实，不能把“检测到合成扰动”等同于“语音专家应被移除”或“融合预测必须拒绝”。

笑容特征置换下，去笑容改善 log-loss 0.03928；但五折中仍有一折的事后最佳固定动作是 UFNet。这些故障描述只对应已有特征/分数合成场景，不是原始音视频真实故障验证。

## 4. 指标排序不同，需要明确研究目标

正常合并外层预测中，均值 AUROC=0.89057、log-loss=0.42392；UFNet AUROC=0.85528、log-loss=0.39883。更好的排序能力与更低的概率损失并不等价，不能因 UFNet 的 log-loss 更低就说它 AUROC 更好，也不能仅凭这两项差异断定原因就是校准。

本报告 AUROC 来自合并五折预测，与此前逐折 AUROC 均值不是同一统计口径；例如此前 UFNet 的逐折均值为 0.8604。本次额外保存每折指标，避免混用。

## 后续决策

结论是“存在互补和当前动作集合的事后空间，但尚未证明可以识别或利用”，不是 Agent 已有效。

下一步建议实验二：暂不加入拒绝，在完整隔离框架下比较均值、正则化逻辑回归融合、一个低复杂度自适应候选；检查加入质量特征是否产生增量收益。模型输入只能使用部署可得信息，不能使用场景名、真实标签或理想动作选择结果作为推理输入。事后外层结果可用于提出假设，不能据其选参数后当作独立确认。

若简单融合最好，采用简单融合；若理想空间存在但收益无法预测，停止当前路由扩张并调查特征信息不足。不得直接用本次表格制作“按场景选择最佳动作”的已验证模型。不启动大模型训练。

# 实验二补充：校准对照与强语音噪声失败诊断

2026-09-05。已完成五折、10 个单变量校准器。复用实验二冻结的逻辑回归模型，没有重新训练它，也没有启用拒绝或查看锁定测试集。

## 协议与检查

脚本：`tools/run_fusion_calibration_diagnostic.py`。新增对照分别对可用专家均值、UFNet 分数做 logit 后的单变量逻辑回归校准（训练侧标准化，C=1，max_iter=2000）。成员、基础 OOF 缓存和训练权重与实验二一致：每人正常权重 0.5，14 个允许拟合扰动合计 0.5；反向笑容冲突不拟合。所有输出均全覆盖。

拟合前写入协议与输入/源码散列；核对实验二私有模型和预测散列，在本次新进程中复现了冻结逻辑回归的外层分数。五折成员无重叠，总计 632 人。10 个标量校准斜率均为正，每折每场景校准前后 AUROC 一致；序列化往返一致。冻结逻辑回归的加性 logit 分解也通过数值检查。运行前后输入不变，下载后聚合文件与脚本散列一致。

## 1. 简单校准已能解释为何需要更强的基线

正常数据五折等权平均：

| 方法 | AUROC | log-loss（低好） | 10-bin ECE（低好） |
| --- | ---: | ---: | ---: |
| 原始等权均值 | 0.890535 | 0.423866 | 0.125755 |
| 均值 + 单变量校准 | 0.890535 | 0.360986 | 0.079395 |
| 完整逻辑回归融合 | 0.889725 | 0.365075 | 0.075003 |
| UFNet | 0.860433 | 0.398815 | 0.087198 |
| UFNet + 单变量校准 | 0.860433 | 0.405697 | 0.087596 |

按参与者汇总的配对 log-loss 收益（正值表示改善）：

| 比较 | 正常收益 [描述性 95% 区间] | 14 压力场景收益 [描述性 95% 区间] |
| --- | --- | --- |
| 校准均值 vs 原均值 | 0.062846 [0.047406, 0.078704] | 0.027891 [0.014420, 0.041380] |
| 完整逻辑回归 vs 校准均值 | -0.004068 [-0.016873, 0.009016] | -0.011740 [-0.023184, 0.000114] |

校准均值正常和平均压力收益在 5/5 折为正；完整逻辑回归相对校准均值正常仅 1/5 折为正、平均压力 0/5 折为正。后者区间仍包含零，不应称已证明统计劣效。

这支持“复杂融合尚未证明超过简单校准”，不能精确归因完整逻辑回归收益有多少百分比来自校准；两个模型不是可加的因果分解。单变量校准也包含截距/先验调整，且 ECE 并非所有方法排序都与 log-loss 一致。UFNet 再校准没有观察到收益，校准不是普遍必然有效。

合并五折正常 AUROC：原均值 0.890572、校准均值 0.892068、完整逻辑回归 0.880932。单调变换保持每折排序，但不同折使用不同变换，合并后的跨折排序可以变化。这解释了统计口径为何可能不同，不等于已确定实验二逻辑回归跨折差异的唯一原因。

## 2. 校准并没有解决语音高斯扰动

在三个语音高斯噪声场景平均，校准均值相对原均值收益 -0.043496，区间 [-0.060026, -0.027090]；完整逻辑回归相对校准均值再下降 0.039101，收益区间 [-0.058016, -0.020731]。相比之下，语音遮蔽场景校准均值改善 0.057751。

最强语音高斯场景（1.0），632 人合并结果：

| 方法 | log-loss | 错误率（阈值 0.5） | 高置信度错误人数 |
| --- | ---: | ---: | ---: |
| 原始等权均值 | 0.527137 | 25.79% | 2 |
| 校准均值 | 0.589831 | 33.39% | 13 |
| 完整逻辑回归 | 0.656163 | 31.80% | 36 |

高置信度错误定义：判错且所预测类别概率至少 0.9。原均值、校准均值、完整逻辑回归的这类错误对全体平均 log-loss 的贡献分别为 0.007620、0.062203、0.178651。因此明确存在高置信度错误负担增加；不能只归因错误人数，也不能把有害的概率放大解释成排序指标改善。

均值校准的 logit 斜率为 1.8559–2.4551，会扩展原始 logit 尺度，但带有截距，不意味着每个样本都更自信。在正常分布能降低损失，在高斯扰动中则没有保持这一优势。

冻结完整逻辑回归相对正常场景的特征贡献变化，按折平均的绝对 logit 变化：语音 0.9659、UFNet 0.4818、笑容 0.0559、手指 0.0343，可用性项为零。代数上主要变化来自语音及依赖语音的 UFNet 通道。它们不是独立信息源，以上不是因果重要性比例，也不能据此直接删除一个通道；非受扰专家仍有 MC 推理变化。

## 3. 决策与后续实验边界

1. 后续研究必须增加“校准均值”强基线；不能只比原均值就把概率损失改善归功于 Agent。
2. 不晋级完整逻辑回归，也不把校准均值宣布为通用鲁棒模型。它在高斯语音场景明显恶化。
3. 若开展下一轮鲁棒性实验，应优先围绕“在训练侧识别任务损失恶化，并控制概率放大”的窄问题；一次只改变一个因素，并与原均值、校准均值同时比较。
4. 拒绝实验如开展，必须另行固定预测器、覆盖率和评价协议，不得用全拒绝隐藏此次失败。当前没有启动拒绝模块新训练或大模型训练。

现阶段证据更支持校准/分布变化问题，而非增加 Agent 复杂度。所有结果来自已经暴露的开发数据；1000 次按参与者、折和类别分层 bootstrap 为描述性区间，未重新拟合也未进行多重比较校正，不能替代独立确认。

## 产物

服务器 `/root/autodl-tmp/experiments/PARK_repro_01/results/fusion_calibration_diagnostic_20260905/` 保存完整模型、私有预测和报告数据。本地 `results/fusion_calibration_diagnostic_20260905/` 保存 protocol、summary、metrics、comparisons、coefficients、logit_shifts。拟合与分析阶段约 2.92 秒，不含缓存读取、既往基础训练和传输。
